{
  "name": "iattualita-sito",
  "version": "1.0.0",
  "private": true,
  "description": "iattualita.it — L'informazione intelligente e in tempo reale",
  "scripts": {
    "build": "esbuild app.jsx --loader:.jsx=jsx --minify --target=es2017 --outfile=app.js",
    "watch": "esbuild app.jsx --loader:.jsx=jsx --minify --target=es2017 --outfile=app.js --watch"
  },
  "devDependencies": {
    "esbuild": "0.25.10"
  }
}
