{
  "name": "iattualita-sito",
  "version": "1.0.0",
  "private": true,
  "description": "iattualita.it — L'informazione intelligente e in tempo reale",
  "scripts": {
    "build": "esbuild app.jsx --bundle --format=iife --loader:.jsx=jsx --minify --target=es2017 --outfile=app.js",
    "test": "node test/subscribe.test.js && node test/digest.test.js && node test/optin.test.js && node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON test/prerender.test.mjs",
    "watch": "esbuild app.jsx --bundle --format=iife --loader:.jsx=jsx --minify --target=es2017 --outfile=app.js --watch"
  },
  "devDependencies": {
    "esbuild": "0.25.10"
  }
}
